{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<div>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}
    <button>{{ 'validate'|trans({}, 'common') }}</button>
    {{ form_end(form) }}
</div>
{% endblock %}
